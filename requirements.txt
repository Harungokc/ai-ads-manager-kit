# NOT: MCP server'lar hosted servisler (npx/pip ile kurulmaz) — bkz. docs/MCP-KURULUM.md.
# Aşağıdakiler yalnızca SELF-HOST veya yerel python scriptleri/analiz içindir.

# Meta Ads MCP — yalnızca self-host edersen gerekir (hosted kullanıyorsan kaldır):
# meta-ads-mcp @ git+https://github.com/pipeboard-co/meta-ads-mcp.git

# Google Ads (self-host / doğrudan API scriptleri)
google-ads>=22.0.0
google-analytics-data>=0.18.0

# MCP
mcp>=1.0.0
mcp-server>=0.5.0

# Analytics
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0

# HTTP & API
requests>=2.31.0
httpx>=0.25.0

# Environment
python-dotenv>=1.0.0

# Utilities
tabulate>=0.9.0
rich>=13.0.0
